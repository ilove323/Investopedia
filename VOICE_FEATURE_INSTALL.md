# 💬 聊天页面语音输入功能安装指南

## 🎯 功能说明

在智能问答页面新增**实时语音录音**功能：
- 📍 位置：聊天输入框上方右侧
- 🎤 功能：点击录音 → 自动识别 → 填充到聊天框
- 🔊 支持：仅实时录音（不支持上传文件）
- 🌐 语言：中文识别（基于Whisper服务）

## 📦 安装步骤

### 1. 激活虚拟环境
```bash
cd /Users/laurant/Documents/github/Investopedia
source venv/bin/activate
```

### 2. 安装新依赖
```bash
pip install audio-recorder-streamlit
```

### 3. 验证安装
```bash
python -c "import audio_recorder_streamlit; print('✅ 安装成功')"
```

### 4. 启动应用
```bash
streamlit run app.py
```

## 🎮 使用方法

### 在聊天页面
1. 进入 **💬 智能问答** 页面
2. 在输入框上方右侧看到 **🎤 语音输入** 按钮
3. 点击麦克风图标开始录音（浏览器会请求麦克风权限）
4. 再次点击停止录音
5. 系统自动调用Whisper识别
6. 识别结果自动填充到聊天框并发送

### 浏览器权限
首次使用时，浏览器会弹出麦克风权限请求：
- ✅ Chrome/Edge: 点击"允许"
- ✅ Firefox: 点击"允许"
- ✅ Safari: 点击"允许"
- ⚠️ 必须使用HTTPS或localhost才能访问麦克风

## 🔧 技术架构

### 数据流程
```
用户录音 → audio-recorder-streamlit（浏览器录音）
         ↓
      WAV音频bytes
         ↓
    临时文件保存
         ↓
   Whisper客户端转写
         ↓
   提取识别文本
         ↓
  自动发送到聊天
```

### 关键代码
- **前端录音**: `audio_recorder_streamlit.audio_recorder()`
- **语音识别**: `WhisperClient.transcribe()`
- **聊天集成**: `st.session_state.voice_question`

## ⚠️ 故障排查

### 问题1: 语音按钮不显示
**症状**: 只显示"⚠️ 语音功能不可用"

**解决**:
```bash
pip install audio-recorder-streamlit
streamlit cache clear
```

### 问题2: 录音后无反应
**症状**: 点击录音按钮，但没有识别结果

**检查**:
1. Whisper服务是否运行
   ```bash
   curl http://localhost:9099/
   ```
2. 查看日志中的错误信息
   ```bash
   tail -f logs/app.log
   ```

### 问题3: 浏览器无法访问麦克风
**症状**: 点击录音按钮，浏览器拒绝访问

**解决**:
1. 确保使用 `http://localhost:8501` 而非 IP 地址
2. 或配置 HTTPS（生产环境）
3. 检查浏览器麦克风权限设置

### 问题4: 识别结果不准确
**症状**: Whisper识别的文本与说话内容不符

**优化**:
1. 说话清晰，避免环境噪音
2. 录音时长适中（5-30秒）
3. 检查Whisper模型配置（config.ini）

## 📝 代码修改记录

### 1. requirements.txt
```diff
+ audio-recorder-streamlit>=0.0.10
```

### 2. src/pages/chat_page.py
- 导入 `audio_recorder_streamlit`
- 导入 `WhisperClient`
- 添加语音录音UI（输入框上方）
- 集成Whisper识别逻辑
- 自动填充识别结果到聊天

## 🎨 UI布局

```
┌─────────────────────────────────────────────┐
│  💬 智能问答                                 │
│  基于RAGFlow向量检索 + 知识图谱的政策智能咨询 │
├─────────────────────────────────────────────┤
│                                             │
│  [聊天历史记录显示区域]                      │
│                                             │
├─────────────────────────────────────────────┤
│  ────────────────────────────────────────── │ 
│  [                   ] 🎤 语音输入           │
│   ↑                    ↑                    │
│  占位列            录音按钮区                │
├─────────────────────────────────────────────┤
│  请输入您的问题... [发送]                    │
└─────────────────────────────────────────────┘
```

## 🚀 未来优化

- [ ] 支持实时语音流式识别
- [ ] 添加语音播放功能（回听录音）
- [ ] 支持多语言切换（英文、日文等）
- [ ] 添加语音质量检测
- [ ] 支持长语音自动分段

## 📚 参考文档

- [audio-recorder-streamlit](https://github.com/stefanrmmr/streamlit_audio_recorder)
- [Whisper API](https://platform.openai.com/docs/api-reference/audio)
- [Streamlit Components](https://docs.streamlit.io/develop/concepts/custom-components)
